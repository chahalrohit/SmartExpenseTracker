import { moderateScale, scale, verticalScale } from 'react-native-size-matters';

export const horizontal = moderateScale(15);
export const vertical = verticalScale(15);
export const borderRadius1 = scale(5);
export const borderRadius2 = scale(10);

export const moneySign = '₹';
